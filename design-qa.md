# CHANSTONE 이식 검증

final result: passed

검증일: 2026-09-05. 공개 원본과 Framer 편집기의 레이어·Hero 속성을 참조하고, Codex 브라우저에서 실제 로컬 화면을 열어 확인했습니다.

## 비교 증거

원본: https://chanstone.framer.website/ 및 `/projects/dssystem`.
로컬: http://127.0.0.1:4173/ 및 http://127.0.0.1:4174/portfolio/.

증거는 작업 폴더의 `evidence/screenshots/`에 보관합니다. 이미지 내용의 추가 분석은 사용자 요청에 따라 수행하지 않았습니다. 증거 스크린샷은 업로드 ZIP에 포함하지 않습니다.

| 비교 | 원본 / 구현 | 함께 비교한 이미지 |
|---|---|---|
| 메인 desktop | `source-home-desktop.png` / `local-home-desktop.png` | `compare-home-desktop.jpg` |
| 메인 mobile | `source-home-mobile.png` / `local-home-mobile.png` | `compare-home-mobile.jpg` |
| 상세 desktop | `source-detail-desktop.png` / `local-detail-desktop.png` | `compare-detail-desktop.jpg` |
| 상세 mobile | `source-detail-mobile.png` / `local-detail-mobile.png` | `compare-detail-mobile.jpg` |
| 상세 글꼴·줄바꿈 확대 | 동일 desktop 이미지의 제목·본문 영역 | `compare-detail-type.jpg` |

Desktop CSS viewport 및 원본/구현 PNG 크기: 1280×800.
Mobile CSS viewport 및 원본/구현 PNG 크기: 390×844.
저장된 PNG는 CSS 크기와 동일한 1:1 픽셀 크기이며 리사이즈 없이 나란히 합성했습니다. 합성 파일 위 25px는 비교 라벨입니다. 확대 비교는 원본 크기의 영역을 잘라 위아래로 배치했습니다.
상태: 첫 화면 로딩·글자 효과가 안정된 시점. TV 영상 프레임, 화면 질감 영상, 시각 표시는 시간에 따라 달라지므로 픽셀 일치 판정에서 제외했습니다.

## 필수 비교 항목

- **글꼴·타이포그래피:** 원본 폰트를 로컬 로드. 제목의 굵기, 행간, 본문 줄바꿈이 비교 이미지에서 일치합니다. 상세 desktop 제목 x=136, y=120, width=1008, height=44; mobile x=32, y=96, width=326, height=88.
- **간격·구조:** 헤더, Hero 문자·TV 비율, desktop 상세 본문 폭, mobile 좌우 32px 여백이 일치합니다. 원본 모바일 상세는 첫 커버 이미지를 숨기는 디자인이며 그대로 유지했습니다.
- **색상:** 원본 CSS 토큰, 흑백 본문, 배경 전환을 보존했습니다. 프레임마다 변하는 노이즈 영상의 밝기 차이는 예상된 차이입니다.
- **이미지:** 원본 미디어를 그대로 사용하며 임의 도형이나 대체 이미지로 그리지 않았습니다. 최종 브라우저 점검에서 깨진 이미지 0, 상세 이미지 외부 URL 0.
- **문구:** 원본 문구·기존 오탈자를 보존했습니다. 문의 폼의 메일 초안 안내는 자체 호스팅에 필요한 의도된 변경입니다.

## 수정 이력

1. **[P0, 해결] 상세 페이지 빈 화면.** CMS URL을 계산하던 코드에 상대 URL이 들어가 Invalid base URL 오류가 발생했습니다. 로컬 URL을 origin 기준 절대 URL로 계산하도록 수정했습니다. 수정 후 5개 상세 페이지의 제목·본문을 실제 브라우저에서 확인했습니다.
2. **[P1, 해결] 페이지 전환 중 CMS 데이터 조회 오류.** Framer 전용 `?range=` 응답과 일반 정적 서버 응답의 차이로 Unexpected response length 오류가 발생했습니다. 작은 CMS 파일 전체를 읽고 브라우저에서 필요한 바이트 구간을 추출하도록 수정했습니다. CMS 데이터 내부 URL 치환도 원래 바이트 길이를 유지합니다. `/portfolio/projects/dssystem/` → WORK → `/portfolio/#work` 전환 후 신규 console error/warn 0, 프로젝트 링크 5개, 깨진 이미지 0을 확인했습니다. 실제 배포 번들의 CMS 읽기 함수를 대상으로 정적 응답 회귀 검사도 통과했습니다.
3. **[요청 반영] Made in Framer 플로팅 제거.** 모든 HTML에서 배지 컨테이너를 제거하고 런타임의 배지 마운트를 비활성화했습니다. 메인·상세·모바일 메뉴 전환에서 배지 컨테이너가 존재하지 않음을 확인했습니다. 비교 이미지의 원본 쪽 배지는 의도된 차이입니다.

## 동작 검증

- 메인 로딩, TV 영상 재생, 스크롤 변화, COLUMN/LIST 전환.
- 5개 상세 URL의 직접 접속 및 본문 표시, 상세에서 WORK 이동.
- `/portfolio/` 하위 경로의 상세 직접 접속 및 메인 이동.
- 모바일 메뉴 열기, 연락처 이동 후 메뉴 닫힘.
- 빈 문의 폼의 브라우저 필수 입력 검증. 메일 초안의 한글·특수문자·줄바꿈은 별도 로컬 테스트로 검증. 실제 메일 발송은 수행하지 않았습니다.
- 원본 404 화면과 BACK TO HOME 링크 확인.
- 7개 경로, 287개 로컬 리소스, 1,678개 정적 참조 검사 통과. CMS 바이트 길이 보존, 외부 CMS 이미지 제거, 배지 제거 검사 통과.

## 한계 및 후속 사항

발견된 미해결 P0/P1/P2는 없습니다. 전체 비공개 Framer 설정을 개별 검사한 것은 아닙니다. 정적 사이트 이식의 범위에서 검증했습니다. 모든 OS·브라우저 조합과 실제 GitHub 배포 환경은 테스트하지 않았으며, GitHub 업로드 및 공개 배포는 수행하지 않았습니다.

문의 폼은 메일 초안 방식입니다. 원본 공개 실행 모듈을 보존한 결과물이므로 편집 가능한 새 React 컴포넌트 프로젝트는 아닙니다. 자주 바뀌는 본문을 위한 콘텐츠 분리는 추후 개선 사항입니다.

## 완료 항목

- [x] 원본/구현의 같은 화면 크기 비교
- [x] 글꼴·간격·색상·이미지·문구 확인
- [x] 상세 CMS와 정적 호스팅 호환성 수정
- [x] 데스크톱·모바일 주요 동작 확인
- [x] 배지 제거와 재등장 방지
- [x] 루트/하위 경로 빌드 검사

## 동일 탭 이동 변경 (2026-09-05)

사용자 요청으로 WORK 카드·목록의 새 탭 설정을 해제했습니다. 초기 HTML과 실행 후 컴포넌트에 모두 반영했습니다. 메인 카드 5개의 target이 비어 있음을 확인하고, MTS 카드를 클릭한 동일한 브라우저 탭에서 상세 제목이 표시되는 것을 확인했습니다. `/Chan-s-Portfolio/` 경로 빌드·정적 참조·CMS·폼 검사 통과.

## 리소스 압축 패키징 (2026-09-06)

325개 리소스를 9개 gzip 묶음으로 저장하고 첫 빌드 시 자동 복원하도록 구성했습니다. 큰 리소스 4개는 개별 파일로 유지합니다. 복원할 경로와 SHA-256을 검증합니다. 파일이 없는 깨끗한 임시 폴더에서 복원·빌드·정적 검사·CMS 검사를 통과했으며, 복원한 전체 329개 파일이 원본과 SHA-256 기준으로 동일함을 확인했습니다. 사이트의 생성 결과와 동작 변경은 없습니다.
